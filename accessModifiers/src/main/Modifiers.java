package main;

public class Modifiers {
	/* Os modificadores de acesso podem ser utilizados tanto para 
	 * classes quanto para métodos e atributos. Caso uma classe seja 
	 * declarada sem nenhum modificador de acesso (default), 
	 * esta classe é acessada apenas por classes que estejam dentro 
	 * do mesmo pacote. Caso seja declarada como pública (public), 
	 * esa pode ser acessada por qualquer outra parte da aplicação. */
}
