package main;

public class Modifiers {
	/* Informações encapsuladas dentro de classes, como atributos, métodos 
	 * e construtores podem ser também modificadas. 
	 * 
	 * Uma classe marcada como 'public' disponibiliza as informações para 
	 * todas as classes.
	 *  
	 * Uma classe marcada como 'private' disponibiliza as informações apenas 
	 * para a própria classe.
	 * 
	 * Uma classe SEM MARCAÇÃO 'default' disponibiliza as informações apenas 
	 * dentro do mesmo pacote. 
	 * 
	 * Uma classe marcada como 'protected' disponibiliza as informações apenas 
	 * dentro do mesmo pacote e para subclasses. */
}
