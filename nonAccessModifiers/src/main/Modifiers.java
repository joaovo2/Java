package main;

public class Modifiers {
	/* 	Os modificadores de Não - Acesso são modificadores que impedem 
	 * as classes de serem instanciadas ou herdadas. 
	 * 
	 * Quando uma classe é declarada como 'final' a classe não pode ser 
	 * herdada por outra classe. 
	 * 
	 * Quando uma classe é declarada como 'abstract' a classe não pode ser 
	 * instanciada para um objeto. */
}
