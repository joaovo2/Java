package main;

public class Inheritance {
	/* A herança é uma forma de passar atributos e métodos de uma classe 
	 * para outra classe sem precisar repetir códigos, utilizando a palavra 'extends'. 
	 * Neste conceito a classe que utiliza o comando 'extends' é a classe que  vai 
	 * receber os atributos e métodos e se chama subclasse ou classe filho. A classe 
	 * que vai após a palavra chave é a superclasse ou classe pai. */
}
